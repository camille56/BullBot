services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bullbot
      POSTGRES_PASSWORD: bullbot
      POSTGRES_DB: bullbot
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
